import { Test, TestingModule } from '@nestjs/testing';
import { <%= props.nameStudly %>Validator } from './<%= props.nameKebab %>-validator.service';
import bootstrapModules from '../../bootstrap.module';
import { <%= props.nameStudly %>Module } from '../../modules/<%= props.nameKebab %>/<%= props.nameKebab %>.module';

describe('<%= props.nameStudly %>Validator', () => {
    let service: <%= props.nameStudly %>Validator;
    
    beforeEach(async () => {
        const module: TestingModule = await Test.createTestingModule({
            imports: [...bootstrapModules(), <%= props.nameStudly %>Module],
            providers: [<%= props.nameStudly %>Validator],
        }).compile();

        service = module.get<<%= props.nameStudly %>Validator>(<%= props.nameStudly %>Validator);
    });

    it('should be defined', () => {
        expect(service).toBeDefined();
    });
});